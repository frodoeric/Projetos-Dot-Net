<Window x:Class="CadastroDeClientes.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <ListBox HorizontalAlignment="Left" Name="ListaDeClientes" Height="299" Margin="10,10,0,0" VerticalAlignment="Top" Width="258">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Id: "/>
                            <TextBlock Text="{Binding Id}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Nome: "/>
                            <TextBlock Text="{Binding Nome}" />
                        </StackPanel>     
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Sobrenome: "/>
                            <TextBlock Text="{Binding Sobrenome}" />
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button Content="Button" HorizontalAlignment="Left" Margin="273,10,0,0" VerticalAlignment="Top" Width="75" Height="46" Click="Button_Click_1"/>

    </Grid>
</Window>
